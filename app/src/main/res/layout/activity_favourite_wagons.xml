<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.LinearLayoutCompat xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorMainBackground"
    tools:context=".ui.smgr.favourite.FavouriteWagonsActivity"
    android:fitsSystemWindows="true">    <!-- ← добавить -->>

    <include
        android:id="@+id/toolbar"
        layout="@layout/toolbar" />

    <!--
    CHANGED — recyclerView.
    1. layout_height теперь match_parent (вместо wrap_content) — чтобы
       список занимал всю доступную высоту, иначе при удалении
       последнего видимого элемента RecyclerView "схлопывается" вверх,
       создавая впечатление моргания.
    2. animateLayoutChanges="false" на RecyclerView — отключает
       LayoutTransition родителя, который дублирует анимацию удаления
       и создаёт конфликт с ViewPropertyAnimator из адаптера.
-->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="false"
        android:clipToPadding="false"
        android:paddingStart="4dp"
        android:paddingEnd="4dp"
        android:paddingTop="6dp"
        android:paddingBottom="16dp"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2" />

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone" />

    <androidx.appcompat.widget.LinearLayoutCompat
        android:id="@+id/layoutNotConnection"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/txtView_check_internet_connection" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btnClickReply"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/txtView_repeat_request" />

    </androidx.appcompat.widget.LinearLayoutCompat>


</androidx.appcompat.widget.LinearLayoutCompat>